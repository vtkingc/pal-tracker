apiVersion: v1
kind: Service
metadata:
  name: pal-tracker
  labels:
   app: pal-tracker
   name: pal-tracker-pod
spec:
  type: ClusterIP
  selector:
    app: pal-tracker-pod
  ports:
    - port: 8080
